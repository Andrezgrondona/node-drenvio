<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= brand %></title>
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }
        .card {
            text-align: center;
            padding: 20px;
            box-shadow: 0 8px 16px 0 rgba(0, 0, 0, 0.2);
            max-width: 400px;
            background-color: #f5f5f5;
            border-radius: 10px;
            font-family: Arial, sans-serif;
        }
        h1 {
            font-size: 24px;
            color: #333; 
        }
        p {
            font-size: 18px;
            color: #555; 
        }
        .special-price {
            color: red; 
        }
    </style>
</head>
<body>
    <div class="card">
        <h1><%= brand %></h1>
        <p><%= name %></p>
        <p class="special-price"><%= precio %></p>
        <p>$ <%= precioProducto %></p>
    </div>
</body>
</html>
